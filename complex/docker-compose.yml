version: "3"
services:
  client:
    image: "eduardogoncalves551/multi-client-03-01"
    mem_limit: 128m
    hostname: client
  server:
    image: "eduardogoncalves551/multi-server-03-01"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "eduardogoncalves551/multi-worker-03-01"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: "eduardogoncalves551/multi-nginx-03-01"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"